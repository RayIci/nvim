# extract-commitsmith-plugin

Extract headless commit-message generation out of `lua/plugins/sidekick.lua` into a self-contained `commitsmith` plugin with codex/claude/copilot support, a globally persisted harness+model setting, an interactive refinement chat window, and a lean (tool-free) invocation mode
